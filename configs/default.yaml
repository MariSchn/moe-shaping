target:
  num_pieces: 2
  domain: [-1, 1]

model:
  num_experts: 2
  input_dim: 1
  output_dim: 1
  router_top_k: 1

training:
  num_steps: 1000
  batch_size: 100
  learning_rate: 0.01

logging:
  log_plots_every_n_steps: 100

wandb:
  enabled: true
  project: moe-shaping
  entity: mari-schn
  run_name: debug
  run_tags: []
